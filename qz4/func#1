#include <stdio.h>

void swap(int* a, int* b) {
    int temp = *a;
    *a = *b;
    *b = temp;
}

int main() {
    int n = 1;
    int m = 2;

    swap(&n, &m);

    printf("after swap, n = %d, m = %d\n", n, m);
    
    return 0;
}
